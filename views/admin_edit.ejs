<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Booking</title>
    <style>
        /* Reset some default styles */
        body, h1, table, form, input, label {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* General body styling */
        body {
            font-family: Arial, sans-serif;
            line-height: 0;
            background-color: #f4f4f4;
            color: #333;
        }

        /* Header styling */
        header {
            background-color: #333;
            color: #fff;
            padding: 1rem;
            text-align: center;
        }

        header h1 {
            margin: 25px;
        }

        /* Main content styling */
        main {
            padding: 2rem;
        }

        /* Form styling */
        form {
            background-color: #fff;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin: 0.5rem 0;
            font-weight: bold;
        }

        input[type="text"], input[type="radio"], input[type="checkbox"] {
            margin: 0.5rem 0;
            padding: 0.5rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            width: calc(100% - 2rem);
        }

        input[type="radio"], input[type="checkbox"] {
            width: auto;
            margin-right: 0.5rem;
        }

        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 0.75rem 1.5rem;
            cursor: pointer;
            border-radius: 4px;
            font-size: 1rem;
            margin-top: 1rem;
        }

        button:hover {
            background-color: #0056b3;
        }

        /* Footer styling */
        footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 1rem;
            position: fixed;
            width: 100%;
            bottom: 0;
        }
    </style>
</head>
<body>
    <header style="height: 3pc;">
        <h1>Edit Booking - ID <%= booking.id %></h1>
    </header>
    
    <main>
        <form action="/admin_edit/<%= booking.id %>" method="POST">
            <label for="nama_pemesan">Nama Pemesan:</label>
            <input type="text" id="nama_pemesan" name="nama_pemesan" value="<%= booking.nama_pemesan %>" required>

            <label>Jenis Kelamin:</label>
            <label><input type="radio" name="jenis_kelamin" value="Laki Laki" <%= booking.jenis_kelamin === 'Laki Laki' ? 'checked' : '' %>> Laki Laki</label>
            <label><input type="radio" name="jenis_kelamin" value="Perempuan" <%= booking.jenis_kelamin === 'Perempuan' ? 'checked' : '' %>> Perempuan</label>

            <label for="nomor_identitas">Nomor Identitas:</label>
            <input type="text" id="nomor_identitas" name="nomor_identitas" value="<%= booking.nomor_identitas %>" required>

            <label for="tipe_kamar">Tipe Kamar:</label>
            <input type="text" id="tipe_kamar" name="tipe_kamar" value="<%= booking.tipe_kamar %>" required>

            <label for="harga">Harga:</label>
            <input type="text" id="harga" name="harga" value="<%= booking.harga %>" required>

            <label for="tanggal_pesan">Tanggal Pesan:</label>
            <input type="text" id="tanggal_pesan" name="tanggal_pesan" value="<%= booking.tanggal_pesan %>" required>

            <label for="durasi_menginap">Durasi Menginap:</label>
            <input type="text" id="durasi_menginap" name="durasi_menginap" value="<%= booking.durasi_menginap %>" required>

            <label><input type="checkbox" name="termasuk_breakfast" <%= booking.termasuk_breakfast ? 'checked' : '' %>> Termasuk Breakfast</label>

            <label for="total_bayar">Total Bayar:</label>
            <input type="text" id="total_bayar" name="total_bayar" value="<%= booking.total_bayar %>" required>

            <button type="submit">Simpan Perubahan</button>
        </form>
    </main>

    <footer style="height: 18px;">
        <p>&copy; 2024 Hotel Booking Admin Panel</p>
    </footer>
</body>
</html>
